{
  "name": "Dotfiles for kiririmode",
  "build": {
    "dockerfile": "Dockerfile",
    "options": [
      "--no-cache"
    ]
  },
  "features": {
    "ghcr.io/devcontainers-extra/features/direnv:1": {},
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
  },
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "editor.formatOnPaste": true,
        "editor.tabSize": 4,
        "editor.insertSpaces": true,
        "editor.codeActionsOnSave": {
          "source.organizeImports": "explicit"
        },
        "files.trimTrailingWhitespace": true,
        "files.insertFinalNewline": true,
        "files.trimFinalNewlines": true,
        "files.autoSave": "onFocusChange"
      },
      "extensions": [
        "ms-vscode.makefile-tools"
      ]
    }
  },
  "postCreateCommand": "echo 'eval \"$(direnv hook bash)\"' >> ~/.bashrc && pre-commit install",
  "postStartCommand": "${containerWorkspaceFolder}/etc/scripts/claude-code.sh",
  "remoteUser": "vscode"
}
